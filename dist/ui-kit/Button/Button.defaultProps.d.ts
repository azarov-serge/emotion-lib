import { ButtonType } from './types';
import { ComponentSize } from '@/ui-kit/@types';
export declare const defaultProps: {
    type: ButtonType;
    size: ComponentSize;
    disabled: boolean;
    block: boolean;
    loading: boolean;
    mb: number;
};
