import { css } from '@emotion/react';
import { TextTheme } from '../../types';

export const text: TextTheme = {
	small: css`
		font-size: 12px;
		line-height: 18px;
	`,
	middle: css`
		font-size: 16px;
		line-height: 28px;
	`,
};
