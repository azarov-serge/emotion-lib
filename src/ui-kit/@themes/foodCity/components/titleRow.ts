import { css } from '@emotion/react';
import { TitleRowTheme } from '../../types';
import { Color } from '../color';

export const titleRow: TitleRowTheme = {
	title: css`
		color: ${Color.Black_Oven};
	`,
	row: css`
		color: ${Color.Black_Oven_35};
	`,
};
